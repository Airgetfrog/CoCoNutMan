enum some_enum {
    prefix = SE,
    values = {a, b}
}

config my_globals {
    info = "Test config pls ignore",
    configfile = true,
    fields = {
        int verbosity = 3 {
            info = "Sets the verbosity",
            options = {-v, --verbosity}
        },
        bool help_enabled {
            options = {-h, --help}
        },
        string list infiles {
            info = "Input files",
            argument
        },
        config opts {
            fields = {
                bool do_something {},
                bool do_something_else {}
            }
        }
    }
}

multioption {
    options = -o,
    fields = {
        opts.do_something = true,
        opts.do_something_else = true
    }
}

targetoptions = {-t, --target}
